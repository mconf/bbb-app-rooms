<% if @room.moodle_group_select_enabled? %>
  <% title = t('scheduled_meetings.new.title_with_group', group_name: @current_group_name) %>
<% else %>
  <% title = t('scheduled_meetings.new.title') %>
<% end %>

<div id="scheduled-meetings-new" class="row">
  <div class="col-12 col-md-10 offset-md-1 col-lg-8 offset-lg-2">
    <h2><%= title %></h2>
    <%= render 'form', scheduled_meeting: @scheduled_meeting %>
  </div>
</div>
