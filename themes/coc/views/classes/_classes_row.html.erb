<% segments.each do |segment| %>
  <% segment['grades'].each do |grade| %>
    <% grade['classes'].each do |klass| %>
      <tr class="d-flex row tr-row">
        <td class="d-flex col-12 col-md-3 align-items-sm-center align-items-md-start classes-item">
          <%= segment['name'] %>
        </td>
        <td class="col-12 col-md-3 classes-item">
          <%= grade['name'] %>
        </td>
        <td class="col-12 col-md-3 classes-item">
          <%= klass['name'] %>
        </td>
        <td class="col-12 col-md-3 classes-item">
           <%= link_to t('coc.classes.schedules'), coc_classes_show_path(class_id: klass['id']), class: "btn btn-schedules" %>
        </td>
      </tr>
    <% end %>
  <% end %>
<% end %>
