<div id="safari-close">
  <h2><%= t('default.room.safari_close.title') %></h2>
  <%= link_to t('default.room.safari_close.return_to_scheduled_meetings'), room_path(@room), class: "btn btn-light mt-4 p-3" %>
</div>
